git --version
git init
git status
git config --global user.name and git config user.name
git config --global user.email and git config user.email

git config user.name
git config user.email

▶️  In This Video Usages Command
► git status
► git add . and git add all
► git commit
► git log
► git log --oneline
► git checkout
► git checkout master

✅IN THIS VIDEO USAGES COMMAND
► git branch "here is the brand name"
► git branch
► git branch -b "here is the brand name"
► git checkout 
► git merge
► git diff
► git diff staged 
► git show 
git branch -d "branch name"


✅ This Video Usages Command:
► git remote add origin "origin URL here"
► git push origin "branch name here"
► git commit


✅ IN This Video Topic
► how generate ssh key in git
► how to add ssh in local repository
► how to add ssh key in github
► how to push github with ssh key
► generate ssh key windows command line
► how add ssh key in git local computer

✅ This Video Topic:
► how to download repository from github using git bash
► how to git pull from github
► how to git fetch with github
► how to git clone using ssh key
► how to git clone using ssh key
► how to git stash 
► how to git clean
► how to make gitignore file

✅ In This Video Usages Command
► git clone (for clone a remote repository from github)
► git fetch (for fetch local repository with remote repository)
► git pull (for download update from github in local repository)
► git stash
► git clean
► touch
► git ssh key
► ssh -t git@github.com